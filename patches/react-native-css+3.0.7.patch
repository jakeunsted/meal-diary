diff --git a/node_modules/react-native-css/dist/commonjs/babel/react-native-web.js b/node_modules/react-native-css/dist/commonjs/babel/react-native-web.js
index 9414040..c0dcc85 100644
--- a/node_modules/react-native-css/dist/commonjs/babel/react-native-web.js
+++ b/node_modules/react-native-css/dist/commonjs/babel/react-native-web.js
@@ -9,6 +9,13 @@ exports.handleReactNativeWebObjectPatternRequire = handleReactNativeWebObjectPat
 var _path = require("path");
 var _allowedModules = require("./allowedModules.js");
 function parseReactNativeWebSource(source, filename) {
+  // Don't rewrite imports made from within react-native-web's own dist files (e.g. AnimatedFlatList
+  // importing the plain FlatList via a relative path). Rewriting these creates a self-referential
+  // circular import back into react-native-web's index.js and crashes with "reading 'default' of undefined".
+  // Only external consumers should get the css-wrapped version.
+  if (filename.includes(`${_path.sep}react-native-web${_path.sep}dist${_path.sep}`)) {
+    return;
+  }
   if (source.startsWith(".")) {
     source = (0, _path.resolve)(filename, source);
     const internalPath = source.split("react-native-web/dist")[1];
diff --git a/node_modules/react-native-css/dist/commonjs/compiler/lightningcss-loader.js b/node_modules/react-native-css/dist/commonjs/compiler/lightningcss-loader.js
index a698a33..cba552b 100644
--- a/node_modules/react-native-css/dist/commonjs/compiler/lightningcss-loader.js
+++ b/node_modules/react-native-css/dist/commonjs/compiler/lightningcss-loader.js
@@ -8,10 +8,21 @@ exports.lightningcssLoader = lightningcssLoader;
 function lightningcssLoader() {
   let lightningcssPath;
 
+  // Prefer the lightningcss copy installed in the app's own project (process.cwd() is the
+  // Expo project root under Metro). This lets apps pin lightningcss (e.g. 1.30.1) locally in a
+  // monorepo where the hoisted root copy is a newer, incompatible version.
+  try {
+    lightningcssPath = require.resolve("lightningcss", {
+      paths: [process.cwd()]
+    });
+  } catch {
+    // Intentionally left empty
+  }
+
   // Try to resolve the path to lightningcss from the @expo/metro-config package
   // lightningcss is a direct dependency of @expo/metro-config
   try {
-    lightningcssPath = require.resolve("lightningcss", {
+    lightningcssPath ??= require.resolve("lightningcss", {
       paths: [require.resolve("@expo/metro-config/package.json").replace("/package.json", "")]
     });
   } catch {
diff --git a/node_modules/react-native-css/dist/commonjs/metro/resolver.js b/node_modules/react-native-css/dist/commonjs/metro/resolver.js
index 314ffa1..08f24c0 100644
--- a/node_modules/react-native-css/dist/commonjs/metro/resolver.js
+++ b/node_modules/react-native-css/dist/commonjs/metro/resolver.js
@@ -42,7 +42,11 @@ function webResolver(resolver, context, moduleName, platform) {
   // Skip anything that isn't importing from `react-native-web`
   !resolution.filePath.includes(`${_nodePath.sep}react-native-web${_nodePath.sep}`) ||
   // Skip internal react-native-web files
-  resolution.filePath.includes(`${_nodePath.sep}react-native-web${_nodePath.sep}dist${_nodePath.sep}vendor`)) {
+  resolution.filePath.includes(`${_nodePath.sep}react-native-web${_nodePath.sep}dist${_nodePath.sep}vendor`) ||
+  // Skip imports made from within react-native-web itself (e.g. index.js re-exporting its own
+  // FlatList) - substituting these creates a self-referential circular import back into
+  // react-native-web's index.js. Only external consumers should get the css-wrapped version.
+  context.originModulePath.includes(`${_nodePath.sep}react-native-web${_nodePath.sep}`)) {
     return resolution;
   }
 
diff --git a/node_modules/react-native-css/dist/module/compiler/lightningcss-loader.js b/node_modules/react-native-css/dist/module/compiler/lightningcss-loader.js
index 10b0a53..07ad999 100644
--- a/node_modules/react-native-css/dist/module/compiler/lightningcss-loader.js
+++ b/node_modules/react-native-css/dist/module/compiler/lightningcss-loader.js
@@ -4,10 +4,21 @@
 export function lightningcssLoader() {
   let lightningcssPath;
 
+  // Prefer the lightningcss copy installed in the app's own project (process.cwd() is the
+  // Expo project root under Metro). This lets apps pin lightningcss (e.g. 1.30.1) locally in a
+  // monorepo where the hoisted root copy is a newer, incompatible version.
+  try {
+    lightningcssPath = require.resolve("lightningcss", {
+      paths: [process.cwd()]
+    });
+  } catch {
+    // Intentionally left empty
+  }
+
   // Try to resolve the path to lightningcss from the @expo/metro-config package
   // lightningcss is a direct dependency of @expo/metro-config
   try {
-    lightningcssPath = require.resolve("lightningcss", {
+    lightningcssPath ??= require.resolve("lightningcss", {
       paths: [require.resolve("@expo/metro-config/package.json").replace("/package.json", "")]
     });
   } catch {
